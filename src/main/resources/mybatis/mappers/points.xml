<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="points">

	<insert id="pointsInsert" parameterType="com.javaex.vo.PointsVo">
				INSERT INTO RESERVATIONS (
						NO,
						RESERVE_NO,
						POINT,
						NAME
				VALUES (
				  		SEQ_RESERVATIONS_NO.nextval,
						#{reserveNo},
						#{point},
						NOWDATE
						)
	</insert>
	<update id="pointsUpdate" parameterType="com.javaex.vo.PointsVo"> 
		UPDATE POINTS
			SET 
			  POINT = #{point}
			WHERE 
			  NO = #{no}
	</update>

</mapper>